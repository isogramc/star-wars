import { FilmsPipe } from './films.pipe';
import { HttpClient } from "@angular/common/http";

describe('FilmsPipe', () => {
  it('create an instance', () => {
    //let http: HttpClient = result;
    //const pipe = new FilmsPipe(http);
    //expect(pipe).toBeTruthy();
  });
});
